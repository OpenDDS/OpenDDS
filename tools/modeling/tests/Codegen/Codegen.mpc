project(*pub) : dcpsexe, dds_model {
  after    += aModel
  libs     += aModel
  libpaths += model
  requires += dummy_label // test is disabled

  Source_Files {
    aPublisher.cpp
  }
}

project(*sub) : dcpsexe, dds_model {
  after    += aModel
  libs     += aModel
  libpaths += model
  requires += dummy_label // test is disabled

  Source_Files {
    aSubscriber.cpp
  }
}
