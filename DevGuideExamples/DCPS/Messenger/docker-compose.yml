version: '2'
services:
  subscriber:
    image: wilsonj/opendds
    command: ["subscriber", "-DCPSConfigFile",  "rtps.ini" ]
    volumes:
      - $PWD:/opt/workspace
  publisher:
    image: wilsonj/opendds
    command: ["publisher", "-DCPSConfigFile",  "rtps.ini" ]
    volumes:
      - $PWD:/opt/workspace
