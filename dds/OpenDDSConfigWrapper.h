/*
 * Distributed under the OpenDDS License.
 * See: http://www.opendds.org/license.html
 */

#ifndef OPENDDS_CONFIG_WRAPPER_H
#define OPENDDS_CONFIG_WRAPPER_H

// OpenDDSConfig.h is generated by the configure script or by CMake
// If you're manually configuring OpenDDS, start by creating an empty file
// named OpenDDSConfig.h in a dds/ directory on the include path.
// See the OpenDDS Developer's Guide and dds/OpenDDSConfig.h.in for details
// regarding what can be customized.
#include <dds/OpenDDSConfig.h>

#ifndef OPENDDS_CONFIG_AUTO_STATIC_INCLUDES
#  define OPENDDS_CONFIG_AUTO_STATIC_INCLUDES 0
#endif

#ifndef OPENDDS_CONFIG_BOOTTIME_TIMERS
#  define OPENDDS_CONFIG_BOOTTIME_TIMERS 0
#endif

#if defined DDS_HAS_MINIMUM_BIT && defined OPENDDS_CONFIG_BUILT_IN_TOPICS
#  if OPENDDS_CONFIG_BUILT_IN_TOPICS
#    error DDS_HAS_MINIMUM_BIT is defined but OPENDDS_CONFIG_BUILT_IN_TOPICS=1
#  endif
#elif defined DDS_HAS_MINIMUM_BIT
#  define OPENDDS_CONFIG_BUILT_IN_TOPICS 0
#elif defined OPENDDS_CONFIG_BUILT_IN_TOPICS
#  if !OPENDDS_CONFIG_BUILT_IN_TOPICS
#    define DDS_HAS_MINIMUM_BIT
#  endif
#else
#  define OPENDDS_CONFIG_BUILT_IN_TOPICS 1
#endif

#if defined OPENDDS_NO_CONTENT_FILTERED_TOPIC && defined OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC
#  if OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC
#    error OPENDDS_NO_CONTENT_FILTERED_TOPIC is defined but OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC=1
#  endif
#elif defined OPENDDS_NO_CONTENT_FILTERED_TOPIC
#  define OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC 0
#elif defined OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC
#  if !OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC
#    define OPENDDS_NO_CONTENT_FILTERED_TOPIC
#  endif
#else
#  define OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC 1
#endif

#if defined OPENDDS_NO_CONTENT_SUBSCRIPTION_PROFILE && defined OPENDDS_CONFIG_CONTENT_SUBSCRIPTION
#  if OPENDDS_CONFIG_CONTENT_SUBSCRIPTION
#    error OPENDDS_NO_CONTENT_SUBSCRIPTION_PROFILE is defined but OPENDDS_CONFIG_CONTENT_SUBSCRIPTION=1
#  endif
#elif defined OPENDDS_NO_CONTENT_SUBSCRIPTION_PROFILE
#  define OPENDDS_CONFIG_CONTENT_SUBSCRIPTION 0
#elif defined OPENDDS_CONFIG_CONTENT_SUBSCRIPTION
#  if !OPENDDS_CONFIG_CONTENT_SUBSCRIPTION
#    define OPENDDS_NO_CONTENT_SUBSCRIPTION_PROFILE
#  endif
#else
#  define OPENDDS_CONFIG_CONTENT_SUBSCRIPTION 1
#endif

#if defined OPENDDS_NO_MULTI_TOPIC && defined OPENDDS_CONFIG_MULTI_TOPIC
#  if OPENDDS_CONFIG_MULTI_TOPIC
#    error OPENDDS_NO_MULTI_TOPIC is defined but OPENDDS_CONFIG_MULTI_TOPIC=1
#  endif
#elif defined OPENDDS_NO_MULTI_TOPIC
#  define OPENDDS_CONFIG_MULTI_TOPIC 0
#elif defined OPENDDS_CONFIG_MULTI_TOPIC
#  if !OPENDDS_CONFIG_MULTI_TOPIC
#    define OPENDDS_NO_MULTI_TOPIC
#  endif
#else
#  define OPENDDS_CONFIG_MULTI_TOPIC 1
#endif

#if defined OPENDDS_SECURITY && defined OPENDDS_CONFIG_SECURITY
#  if !OPENDDS_CONFIG_SECURITY
#    error OPENDDS_SECURITY is defined but OPENDDS_CONFIG_SECURITY=0
#  endif
#elif defined OPENDDS_SECURITY
#  define OPENDDS_CONFIG_SECURITY 1
#elif defined OPENDDS_CONFIG_SECURITY
#  if OPENDDS_CONFIG_SECURITY
#    define OPENDDS_SECURITY
#  endif
#else
#  define OPENDDS_CONFIG_SECURITY 0
#endif

#if defined OPENDDS_NO_OBJECT_MODEL_PROFILE && defined OPENDDS_CONFIG_OBJECT_MODEL_PROFILE
#  if OPENDDS_CONFIG_OBJECT_MODEL_PROFILE
#    error OPENDDS_NO_OBJECT_MODEL_PROFILE is defined but OPENDDS_CONFIG_OBJECT_MODEL_PROFILE=1
#  endif
#elif defined OPENDDS_NO_OBJECT_MODEL_PROFILE
#  define OPENDDS_CONFIG_OBJECT_MODEL_PROFILE 0
#elif defined OPENDDS_CONFIG_OBJECT_MODEL_PROFILE
#  if !OPENDDS_CONFIG_OBJECT_MODEL_PROFILE
#    define OPENDDS_NO_OBJECT_MODEL_PROFILE
#  endif
#else
#  define OPENDDS_CONFIG_OBJECT_MODEL_PROFILE 1
#endif

#if defined OPENDDS_NO_OWNERSHIP_KIND_EXCLUSIVE && defined OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE
#  if OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE
#    error OPENDDS_NO_OWNERSHIP_KIND_EXCLUSIVE is defined but OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE=1
#  endif
#elif defined OPENDDS_NO_OWNERSHIP_KIND_EXCLUSIVE
#  define OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE 0
#elif defined OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE
#  if !OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE
#    define OPENDDS_NO_OWNERSHIP_KIND_EXCLUSIVE
#  endif
#else
#  define OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE 1
#endif

#if defined OPENDDS_NO_OWNERSHIP_PROFILE && defined OPENDDS_CONFIG_OWNERSHIP_PROFILE
#  if OPENDDS_CONFIG_OWNERSHIP_PROFILE
#    error OPENDDS_NO_OWNERSHIP_PROFILE is defined but OPENDDS_CONFIG_OWNERSHIP_PROFILE=1
#  endif
#elif defined OPENDDS_NO_OWNERSHIP_PROFILE
#  define OPENDDS_CONFIG_OWNERSHIP_PROFILE 0
#elif defined OPENDDS_CONFIG_OWNERSHIP_PROFILE
#  if !OPENDDS_CONFIG_OWNERSHIP_PROFILE
#    define OPENDDS_NO_OWNERSHIP_PROFILE
#  endif
#else
#  define OPENDDS_CONFIG_OWNERSHIP_PROFILE 1
#endif

#if defined OPENDDS_NO_PERSISTENCE_PROFILE && defined OPENDDS_CONFIG_PERSISTENCE_PROFILE
#  if OPENDDS_CONFIG_PERSISTENCE_PROFILE
#    error OPENDDS_NO_PERSISTENCE_PROFILE is defined but OPENDDS_CONFIG_PERSISTENCE_PROFILE=1
#  endif
#elif defined OPENDDS_NO_PERSISTENCE_PROFILE
#  define OPENDDS_CONFIG_PERSISTENCE_PROFILE 0
#elif defined OPENDDS_CONFIG_PERSISTENCE_PROFILE
#  if !OPENDDS_CONFIG_PERSISTENCE_PROFILE
#    define OPENDDS_NO_PERSISTENCE_PROFILE
#  endif
#else
#  define OPENDDS_CONFIG_PERSISTENCE_PROFILE 1
#endif

#if defined OPENDDS_NO_QUERY_CONDITION && defined OPENDDS_CONFIG_QUERY_CONDITION
#  if OPENDDS_CONFIG_QUERY_CONDITION
#    error OPENDDS_NO_QUERY_CONDITION is defined but OPENDDS_CONFIG_QUERY_CONDITION=1
#  endif
#elif defined OPENDDS_NO_QUERY_CONDITION
#  define OPENDDS_CONFIG_QUERY_CONDITION 0
#elif defined OPENDDS_CONFIG_QUERY_CONDITION
#  if !OPENDDS_CONFIG_QUERY_CONDITION
#    define OPENDDS_NO_QUERY_CONDITION
#  endif
#else
#  define OPENDDS_CONFIG_QUERY_CONDITION 1
#endif

#if defined OPENDDS_SAFETY_PROFILE && defined OPENDDS_CONFIG_SAFETY_PROFILE
#  if !OPENDDS_CONFIG_SAFETY_PROFILE
#    error OPENDDS_SAFETY_PROFILE is defined but OPENDDS_CONFIG_SAFETY_PROFILE=0
#  endif
#elif defined OPENDDS_SAFETY_PROFILE
#  define OPENDDS_CONFIG_SAFETY_PROFILE 1
#elif defined OPENDDS_CONFIG_SAFETY_PROFILE
#  if OPENDDS_CONFIG_SAFETY_PROFILE
#    define OPENDDS_SAFETY_PROFILE
#  endif
#else
#  define OPENDDS_CONFIG_SAFETY_PROFILE 0
#endif

#if defined OPENDDS_RAPIDJSON && defined OPENDDS_CONFIG_RAPIDJSON
#  if !OPENDDS_CONFIG_RAPIDJSON
#    error OPENDDS_RAPIDJSON is defined but OPENDDS_CONFIG_RAPIDJSON=0
#  endif
#elif defined OPENDDS_RAPIDJSON
#  define OPENDDS_CONFIG_RAPIDJSON 1
#elif defined OPENDDS_CONFIG_RAPIDJSON
#  if OPENDDS_CONFIG_RAPIDJSON
#    define OPENDDS_RAPIDJSON
#  endif
#else
#  define OPENDDS_CONFIG_RAPIDJSON 0
#endif

// If features content_filtered_topic, multi_topic, and query_condition
// are all disabled, define a macro to indicate common code these
// three features depend on should not be built.
#if !OPENDDS_CONFIG_QUERY_CONDITION && !OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC && !OPENDDS_CONFIG_MULTI_TOPIC
#  define OPENDDS_CONFIG_CONTENT_SUBSCRIPTION 0
#endif

// Sanity checks.

#if (OPENDDS_CONFIG_QUERY_CONDITION || OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC || OPENDDS_CONFIG_MULTI_TOPIC) && !OPENDDS_CONFIG_CONTENT_SUBSCRIPTION
#  error OPENDDS_CONFIG_QUERY_CONDITION || OPENDDS_CONFIG_CONTENT_FILTERED_TOPIC || OPENDDS_CONFIG_MULTI_TOPIC = 1 but OPENDDS_CONFIG_CONTENT_SUBSCRIPTION=0
#endif

#if OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE && !OPENDDS_CONFIG_OWNERSHIP_PROFILE
#  error OPENDDS_CONFIG_OWNERSHIP_KIND_EXCLUSIVE=1 but OPENDDS_CONFIG_OWNERSHIP_PROFILE=0
#endif

#endif
