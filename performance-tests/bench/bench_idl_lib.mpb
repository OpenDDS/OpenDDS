project: dcps_rtps_udp {
  after    += Bench_Idl
  libs     += Bench_Idl
  expand(BENCH_ROOT) {
    $BENCH_ROOT
    $(DDS_ROOT)/performance-tests/bench
  }
  libpaths += $(BENCH_ROOT)/lib
  includes += $(BENCH_ROOT)/idl
}
