project: dcpsexe, dcps_test, test_root {
  after       += TestFramework
  libs        += TestFramework
  libpaths    += $(DDS_ROOT)/tests/DCPS/TestFramework
  includes    += $(DDS_ROOT)/tests/DCPS/TestFramework
}
