project: dcps_rtps, dds_openssl, ace_xml_utils {
  after += OpenDDS_Security
  avoids += no_opendds_security

  specific(cmake) {
    libs += ${OPENDDS_SECURITY_LIB}
  } else {
    libs += OpenDDS_Security
  }
}
