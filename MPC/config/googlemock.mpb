project : googletest {

  // For compatibility with tests/gtest_setup.txt instructions
  includes += $(GTEST_ROOT)/../../googlemock/include
  libpaths += $(GTEST_ROOT)/../googlemock/$(Configuration)

  libs += gmock
}
