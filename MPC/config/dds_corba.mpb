feature(!no_opendds_safety_profile) {
  specific(cmake) {
    includes += ${OPENDDS_SAFETY_PROFILE_INCLUDES}
  } else {
    includes += $(DDS_ROOT)/dds/CORBA
  }
  libpaths += $(DDS_ROOT)/lib
  libs += OpenDDS_Corba
  after += OpenDDS_Corba
}
