project: dcps, dcps_ts_defaults {

  avoids += no_cxx11
  dcps_ts_flags += -Lc++11
  idlflags += -SS

  Modify_Custom(Idl) {
    automatic_in = 0
  }

  Modify_Custom(TypeSupport) {
    header_pre_extension += C
  }
}
