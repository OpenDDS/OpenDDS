module Unions {
  struct dummy {
    long foo;
  };

  union Z switch(boolean) {
    case TRUE: short s;
  };

  union Y switch(short) {
    case 0: string q;
  };

  union X switch(long) {
    case 78: short s;
    case 0: Z zz;
  };

  union V switch(short) {
    case 0: dummy d;
  };

  // The generated type support code for U and T does not compile.

  //union U switch(unsigned short) {
  //  case 0: sequence<dummy> dsequence;
  //};

  //union T switch(unsigned short) {
  //  case 0: dummy darray[10];
  //};
};

