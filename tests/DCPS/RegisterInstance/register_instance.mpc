project(*topic): dcpsexe_with_udp, dcps_rtps {
  exename   = register_instance_test
  libs     += DcpsFooType
  libpaths += ../FooType
  after    += DcpsFooType

  Source_Files {
    register_instance.cpp
  }
}
