project (*): dcpsexe, dcps_rtps, googletest {
  requires += no_opendds_safety_profile
  // Temporary avoidance until googletest is built into
  // the broader OpenDDS codebase
  avoids += no_opendds_security
  exename = *
}

